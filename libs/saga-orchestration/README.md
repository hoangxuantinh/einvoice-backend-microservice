# saga-orchestration

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build saga-orchestration` to build the library.

## Running unit tests

Run `nx test saga-orchestration` to execute the unit tests via [Jest](https://jestjs.io).
